import Breadcrumb from "components/Breadcrumb";

const Outcome = () => {
    return (
        <main className="workspace">
            {/* Breadcrumb */}
            <section className="breadcrumb">
                <Breadcrumb title="Summary of Outcome" />
            </section>
        </main>
    );
};

export default Outcome;